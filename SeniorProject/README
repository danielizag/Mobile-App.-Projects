Senior project made as an application for contractor/sub-contractor business models. Alpha version 
was made for the project, however now it is still in progress (for fun). Technologies: Android, Java, 
Custom Web Service (RESTful API). 

Application is made with android studio and java. The web service is made using PHP, where it is providing 
JSON data via URI to transmit (POST) data and receive (GET) data.

Finished: web service, RecyclerView/CardView, Google Maps API, current job Details, job issue submition, 
employee w/ emp. details.

Backlog: Employee Scheduling, CardView OnClickListener, refactor: activities into fragments where possible
and make a single data call into an app-wide bundle. 
